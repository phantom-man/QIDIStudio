# QIDIStudio universal memory venv requirements
# Venv: memory_env\Scripts\python.exe (Python 3.13)
# Install: .\memory_env\Scripts\python.exe -m pip install -r memory\requirements.txt

# Core memory stack
lancedb>=0.29.2  # v0.30.0 betas not on PyPI yet; upgrade when stable releases
sentence-transformers>=3.0.0
python-dotenv>=1.0.0
langsmith>=0.1.0
langchain>=0.2.0
langchain-core>=0.2.0
psycopg2-binary>=2.9.0
psycopg[binary]>=3.1.0
psycopg-pool>=3.2.0
pyarrow>=14.0.0

# Agent fleet (LangGraph + Google Gemini)
langgraph>=0.2.0
langchain-google-genai>=4.2.0
langgraph-checkpoint-postgres>=2.0.0

# PhD Board of Directors pipeline
tavily-python>=0.3.0
langchain-community>=0.2.0
langchain-openai>=0.1.0

# OCP CAD Viewer + build123d (3D part display)
build123d>=0.8.0
ocp_vscode>=2.7.0

# ── PyTorch stack (optional — for MeshStressGNN, NozzleLSTM, TextureMLP) ─────
# CPU-only build (no CUDA required). For GPU: swap for torch==<ver>+cu121 from pytorch.org
# Install separately if needed:
#   memory_env\Scripts\pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2.0
torchvision>=0.17.0

# numpy-stl for fast vertex extraction in manufacturing_graph.py
numpy-stl>=3.1.0
