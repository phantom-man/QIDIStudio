# services/ — QIDIStudio cloud services layer
# db/           Cloud SQL + BigQuery clients and ORM models
# auth/         Device fingerprinting, JWT, subscription gating
# slicer_bridge/ Python sidecar that syncs cloud presets to local cache
